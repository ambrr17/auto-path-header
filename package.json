{
  "name": "auto-path-header",
  "displayName": "Auto Path Header",
  "description": "Automatically inserts the relative path of the file as a comment on the first line. Автоматически вставляет относительный путь файла в виде комментария на первой строке.",
  "version": "0.0.2",
  "author": "Niklis",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "autoPathHeader.insertComment",
        "title": "Insert Path Comment",
        "category": "Auto Path Header"
      }
    ],
    "configuration": {
      "title": "Auto Path Header",
      "properties": {
        "autoPathHeader.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable automatic comment insertion"
        },
        "autoPathHeader.language": {
          "type": "string",
          "enum": ["auto", "en", "ru"],
          "default": "auto",
          "description": "Language for extension messages (auto = system language)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.0.0",
    "vsce": "^2.14.0"
  }
}
